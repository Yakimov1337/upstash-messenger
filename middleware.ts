// export { default } from "next-auth/middleware";

// //Secures the matching routes...
// export const config = { matcher: ["/"] };
export { default } from "next-auth/middleware"

export const config = { matcher: ["/"] }